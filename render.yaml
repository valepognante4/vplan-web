services:
  - type: web
    name: vplan-backend
    env: docker
    region: ohio
    plan: free
    dockerfilePath: ./Dockerfile.backend  # <--- Sin el # adelante
    envVars:
      - key: PORT
        value: 3001

  - type: web
    name: vplan-frontend
    env: docker
    region: ohio
    plan: free
    dockerfilePath: ./Dockerfile.frontend # <--- Sin el # adelante